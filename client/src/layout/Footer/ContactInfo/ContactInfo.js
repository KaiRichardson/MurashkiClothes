import React from 'react';
import styled from 'styled-components';

const ContactInfo = () => {
  return <Wrapper>ContactInfo</Wrapper>;
};

export default ContactInfo;

const Wrapper = styled.section`
  grid-area: ContactInfo;
`;
