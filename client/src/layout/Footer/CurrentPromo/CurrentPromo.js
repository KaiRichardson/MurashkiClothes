import React from 'react';
import styled from 'styled-components';

const CurrentPromo = () => {
  return <Wrapper>CurrentPromo</Wrapper>;
};

export default CurrentPromo;

const Wrapper = styled.section`
  grid-area: CurrentPromo;
`;
